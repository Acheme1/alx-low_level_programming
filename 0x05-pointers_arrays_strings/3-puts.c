#include "main.h"
#include <stdio.h>

/**
* _puts - prints a string, follow by a new line.
*
* @str: string
*
*Return: always 0 success
*
*/

void _puts(char *str = i)

{
	while (*str i = '\0')

	{
		_putchar(*str i++);
	}
	_putchar('\n');

	return(0);
}
