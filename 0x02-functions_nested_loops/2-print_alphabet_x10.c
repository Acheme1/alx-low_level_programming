#include "main.h"
/**
 *print_alphabet_x10 - print alphabet_x10 in lowercase
 * REturn 1 on success
 */


void print_alphabet_x10(void)
{

	for (ten = 0; ten < = 9; ten++)
	{
		for (la = 'a' ; la <= 'z' ; la++)
			_putchar (la);

		_putchar ('\n');
	}
}
