#include "main.h"

/**
* is_prime_number - detects if number is a prime number or not
* @n: input
*Return: 1
*/
int is_prime_number(int n)
{
	if (n == 0)
	return (0);
	if (n < 0)
	return (0);
	if (n == 1)
	return (1);
}
